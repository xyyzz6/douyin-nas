# 多设备同步（账号系统）

多台设备登同一个账号，自动对齐：**点赞 / 收藏 / 坏码流名单 / 昵称头像 / 片源与监控清单**；
strm 备份包也能存进账号，新设备登录后自动恢复（换机不用再手动导 zip）。

**服务端是可选的** —— 不配这一项，App 和以前完全一样，所有数据只在本机。

> 📦 **用飞牛 fnOS 当服务端？** 有现成的应用包（`.fpk`），应用中心里「手动安装」即可，
> 装完桌面点图标就能用、开机自启、数据自动持久化。见 **[FNOS.md](FNOS.md)**。

---

## 一、在 NAS 上把服务端跑起来

### 依赖
- Node.js ≥ 18（`node -v` 看一眼就知道）
- 项目里的 `server.js` **和** `public/` 目录（前端和它是同一份，你平时用的网页版就是它）

### 启动
```bash
cd /volume1/douyin-nas          # 换成你的项目目录
node server.js                  # 默认监听 8080
```

改端口 / 把数据放到别处（推荐：升级代码时不动数据）：
```bash
PORT=8080 NAS_DATA_DIR=/volume1/douyin-data node server.js
```

> 🔄 **升级 App 后记得把这份文件也换掉并重启服务**：合并规则、接口都在它里面。
> 光换 App 不换它，有些行为会停在旧版（例如「删掉的监控文件夹被同步加回来」
> 就是服务端合并规则的问题，App 端改不了）。

### 让它开机自启

群晖「控制面板 → 任务计划」或威联通「自动执行」里加一条：
```
cd /volume1/douyin-nas && PORT=8080 NAS_DATA_DIR=/volume1/douyin-data node server.js
```

systemd（Linux 机器）：
```ini
[Unit]
Description=Douyin NAS sync server
After=network.target

[Service]
WorkingDirectory=/volume1/douyin-nas
Environment=PORT=8080
Environment=NAS_DATA_DIR=/volume1/douyin-data
ExecStart=/usr/bin/node server.js
Restart=always

[Install]
WantedBy=multi-user.target
```

### 确认起来了
浏览器打开 `http://<NAS的IP>:8080`，能看到视频页面就成。

---

## 二、手机上登录

「我的」→「数据源设置」→ 拉到最底 **第 5 步 · 多设备同步**：

| 填什么 | 说明 |
|---|---|
| 同步服务器 | `http://<NAS的IP>:8080`（同一 WiFi 用局域网 IP） |
| 账号 / 密码 | 第一台设备点「注册新账号」，之后每台点「登录」 |

登录后的行为：
- 立刻自动同步一次（把本机的点赞收藏推上去、把账号里的拉下来）
- 以后点赞 / 收藏 / 改昵称，**几秒内自动推**上去
- 每次打开 App 静默拉一次

---

## 三、换新设备

1. 装 App，第 1~4 步照常填（服务地址、选文件夹）
2. 第 5 步登录同一个账号
3. 它会自动：拉点赞收藏 → 补片源与监控清单 → **如果这台设备还没有 strm 库，自动把账号里的备份包拉下来恢复**

⚠️ 备份包要先在**旧设备**上传一次：「第 5 步 → 上传到账号」。
传过一次之后，以后换设备就再也不用手动导 zip 了。

---

## 四、服务器上的数据长这样

```
<数据目录>/
├── config.json              # 网页版的片源配置
├── state.json               # 网页版的点赞收藏
└── sync/                    # 同步账号
    ├── accounts.json        # 账号表：用户名 → scrypt(salt,hash)；token 表
    ├── u-<用户名>.json       # 该账号的数据（点赞/收藏/坏码流/资料/片源）
    └── u-<用户名>.strm.zip   # 该账号的 strm 备份包
```

**备份整个 `sync/` 目录 = 备份所有账号**（含每个人的点赞收藏与 strm 包）。

---

## 五、安全与注意

- 密码只存 **scrypt 哈希**，不存明文；
- 登录凭证是随机 token（180 天有效）。「退出账号」只作废当前设备的 token；「改密码」会作废该账号**全部** token；
- 账号之间数据隔离，谁也看不到谁的；
- 🔴 默认是 **HTTP 明文**（和你的 WebDAV 一样）。要跨公网用，**请在前面挂一层反向代理 + HTTPS**，别把 8080 直接映射到公网；
- 🔴 注册默认开放。如果这台机器能从公网/公司网访问，建议关掉注册：
  ```bash
  SYNC_ALLOW_REGISTER=0 PORT=8080 node server.js
  ```
  （已有账号照常登录；要加新账号时临时去掉这个变量重启一次再关）
- 纯局域网使用不需要任何额外配置。

---

## 六、接口一览（排查 / 自己调着玩）

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/auth/register` | `{user,pass}` → `{token}`（可用 `SYNC_ALLOW_REGISTER=0` 关闭） |
| POST | `/api/auth/login` | 同上 |
| GET | `/api/auth/me` | 校验 token |
| POST | `/api/auth/logout` | 作废当前 token |
| POST | `/api/auth/pass` | `{old,pass}` 改密码（作废该账号全部 token） |
| GET | `/api/sync/pull` | 拉全量数据 + strm 包信息 |
| POST | `/api/sync/push` | 推本机变化，返回**合并后**的全量 |
| GET / PUT | `/api/sync/strm` | 下载 / 上传 strm 备份包（zip） |

请求都要带 `Authorization: Bearer <token>`。例：

```bash
TK=$(curl -s -X POST -H 'Content-Type: application/json' \
     -d '{"user":"me","pass":"1234"}' \
     http://192.168.1.10:8080/api/auth/login | grep -o '"token":"[^"]*"' | cut -d'"' -f4)

curl -s -H "Authorization: Bearer $TK" http://192.168.1.10:8080/api/sync/pull
```

> 排查「同步不上」时，先看这两条：
> 1. 手机浏览器打开 `http://<NAS的IP>:8080` 通不通（同一 WiFi？IP 对不对？）；
> 2. 设置页第 5 步的状态行写了什么 —— 失败原因就在那行字里。

---

## 七、合并规则（多台设备同时改会怎样）

| 内容 | 规则 |
|---|---|
| 点赞 / 收藏 / 坏码流 | **按时间合并**：同一条两边都改过 → 听晚的那次。**取消也会传播**（服务端保留 60 天删除记录，超期就当作没发生过） |
| 片源 / 监控清单 / 不重扫 | **按时间取最后改过的那份**（哪台设备最后改过就以谁为准）。**删掉也会传播** |
| 昵称 / 头像 | 只有**真改过**的那台设备会推，按时间取新；没改过的设备同步**不会**把别人改的盖回去 |
| 自动扫描间隔 | 同上，按时间取最后改过的那份 |

> **2026-09-21 改的**：这三项原来是「并集（只增不减）」。并集**在语义上就表达不了删除** ——
> 你删掉一个监控文件夹，下一次同步就被账号里那份并回来，症状就是
> 「这个文件夹已经不需要监控了但是无法移除」。现在改成按时间戳取最后改过的那份，
> 配套的规则是「只有**真改过**的设备才推」（和昵称/头像同一个思路），
> 所以没改过的设备同步一次不会去抢别人刚做的改动。
>
> ⚠️ **这条规则跑在服务端**（NAS 上那份 `server.js` / `sync-server.js`）。
> **升级 App 之后要把服务端那份文件也换掉、重启服务**，否则删除只能在本机生效、传不到别的设备
> （App 里留了一层兼容：服务端没带时间戳就不采用它回的数组，所以**至少不会**出现
> 「更新完 App 反而更删不掉」）。

---

## 八、不想要了怎么办

- **手机端**：「退出账号」→ 之后所有同步都停，本机数据一点都不动；
- **服务端**：删掉 `sync/` 目录（或干脆不启动同步服务）即可，App 会退回到「纯本机」状态。
