# 飞牛 fnOS 一键安装（.fpk 应用包）

把「NAS 短视频」打包成飞牛的应用包（`.fpk`），在**应用中心 → 手动安装**里选文件即可，
装完飞牛桌面上出现图标，点开就能用；也能拿来当多设备同步的服务端。

> 上架**官方应用商店**要走飞牛开发者平台的提交与审核（需要开发者账号，我这边无法代提交）。
> 自己用/小范围分享，手动安装这一条路就够了，功能完全一样。

---

## 一、选哪个包

**先下载**：去 [**Releases**](https://github.com/xyyzz6/douyin-nas/releases) 找 `nasdouyin-*.fpk` /
`nasdouyinsync-*.fpk`（不用自己打包）。下面两步各挑一个 —— 一共四个包，配对方式是
「用途 × 机型」，比如「只做同步 + x86 小主机」就下 `nasdouyinsync-<版本>-x86.fpk`。

### 先按用途选

| 你要什么 | 包 | 说明 |
|---|---|---|
| **只要一个存点赞收藏的服务端**（推荐） | `nasdouyinsync-<版本>-<架构>.fpk` | 只存资料：点赞 / 收藏 / 坏码流 / 昵称头像 / 片源与监控清单 / strm 备份包。**没有播放器界面、不连你的 WebDAV、不扫你的盘。** 42.5 MB（x86）/ 42.6 MB（arm） |
| 还想要网页版播放器 | `nasdouyin-<版本>-<架构>.fpk` | 完整版：浏览器里就能刷片（WebDAV 浏览 + 播放 + 片源管理），同时也能当同步服务端。50.8 MB（x86）/ 50.9 MB（arm） |

两个包的 appid 不同，**可以同时装、互不冲突**；端口默认都是 8099，同时装的话把其中一个改掉。
（下面所有说明以**同步服务端**为准 —— 完整版多出来的只是播放器部分。）

### 再按机型选

| 后缀 | 机型 | 怎么确认 |
|---|---|---|
| `-x86` | x86_64（大多数 x86 小主机/自组 NAS） | 飞牛「系统设置 → 关于」里看处理器，Intel/AMD 就选它 |
| `-arm` | ARM（N1 / H6 / RK3399 等） | 同上，显示 ARM / RK / 晶晨 就选它 |

两个包**内容完全一样，只有内置的 Node 运行时架构不同** —— 应用零外部依赖，
运行时随包带着，所以不需要你的飞牛上提前装 Node，也不需要 Docker。

> 想不想知道它到底装了什么？同步包里只有三样东西：
> `sync-server.js`（17 KB 的一个文件）、一个 Node 运行时、一份配置。
> 没有网页、没有演示视频、没有任何连你 NAS 的代码。

---

## 二、安装（三步）

1. 打开飞牛桌面 → **应用中心**
2. 右上角 **设置**（齿轮） → **手动安装应用**
3. 选上面的 `.fpk` 文件 → 按向导走

安装向导里只有一项要选：

- **允许注册「多设备同步」账号**（默认开）
  只在局域网用就保持打开；如果这个端口会暴露到公网（做了端口映射或反代），**关掉** ——
  关掉后已有账号照常登录同步，只是不再接受新注册。装好后随时能在应用的「设置」里改。

装完点桌面上的 **NAS 短视频 · 同步服务** 图标会打开一个 JSON 状态页
（`{"ok":true,"service":"nas-douyin-sync",...}`）—— 它不是界面，只是让你确认「它在跑」。

（完整版装完点图标打开的是网页版播放器。）

---

## 三、装完之后

### 1. 手机 App 连它（唯一要做的事）

**我的 → 数据源设置 → 拉到底「第 5 步 · 多设备同步」**

- 服务器地址填 `http://<飞牛的内网IP>:8099`（例如 `http://192.168.1.50:8099`）
- 第一台设备点**注册新账号**，之后每台点**登录**
- 登录后自动同步点赞、收藏、坏码流、片源、监控清单、昵称头像
- 想让 strm 文件也同步：**旧设备点一次「上传到账号」**（约 1.6 MB），
  以后新设备登录就会**自动把 strm 库恢复回来**，不用再手动导 zip

⚠️ 同步服务端**不负责**播片和扫片 —— 那些仍然是你手机上 App 的事，
它只是替你把「我点过哪些赞」这类资料存一份在 NAS 上。

细节见 `SYNC.md`。

### 3. 端口与数据在哪

| 项 | 位置 |
|---|---|
| 监听端口 | **8099**（改端口见下面） |
| 数据目录 | 应用数据目录下的 `data/`（点赞收藏、评论、同步账号、strm 备份包都在这） |
| 运行日志 | 应用数据目录下的 `info.log` |

**升级覆盖安装不会丢数据** —— 数据在 `TRIM_PKGVAR/data`，与应用文件分开。
卸载时飞牛会问是否保留数据；**卸载会连带删掉同步账号**，重装后需要重新注册。

---

## 四、常见问题

**桌面图标点开打不开 / 打不开网页**
先看应用中心里这个应用是不是「运行中」。没在跑的话看日志 `info.log`：
最常见是 **8099 端口被别的程序占了**（飞牛上跑的东西多，容易撞）。

**改端口**
编辑应用数据目录里的 `app.conf`，把 `PORT=8099` 改掉，重启应用。
⚠️ 只改这一处不够 —— 桌面图标跳转的端口写在包里（manifest 与 ui/config），
改了 `app.conf` 后图标可能仍跳旧端口，直接浏览器访问新端口即可。

**ARM 机型装 x86 的包会怎样**
能装上，但一启动就 `exec format error`，应用起不来。选错包就卸掉换另一个。

**想从公网访问**
默认 HTTP 明文。要跨公网用，请在飞牛上加一层反向代理 + HTTPS
（飞牛自带「反向代理 / 证书」功能，或前面再挂一层 Nginx/Caddy），
**不要把 8099 直接端口映射出去**。同时建议把注册关掉。

---

## 五、自己重新打包

```bash
node fnos/build.mjs              # 打 x86 + arm 两个包
node fnos/build.mjs --arch=x64   # 只打其中一个
CODEBUDDY_SAFE_DELETE_ENABLED=0 node fnos/build.mjs   # 若被环境的批量删除保护拦下
```

产物在 `fnos/dist/`。脚本会：

1. 用官方 `fnpack` 做一次结构校验；
2. **自己按 ustar 规范写 tar** —— 官方工具在 Windows 上会把 `cmd/*` 与内置 Node
   打成 0666（没有可执行位），装到飞牛上脚本起不来，所以权限位必须自己写；
3. 把产物解回来自检（权限位、必备条目、manifest 字段、
   **以及 manifest 的 platform 与内置 Node 的 ELF 架构是否自洽**）。

Node 运行时首次会下载到 `fnos/.cache/`，之后复用。
版本号、显示名、端口都在 `fnos/src/manifest`，改完重新打包即可。

---

## 六、目录结构

```
fnos/
├── src/                    # 应用工程（改这里）
│   ├── manifest            # 应用元信息：名称/版本/platform/端口/桌面入口
│   ├── config/
│   │   ├── privilege       # 权限声明
│   │   └── resource        # 资源限制
│   ├── wizard/
│   │   ├── install         # 安装向导表单
│   │   └── config          # 应用设置表单
│   ├── app/ui/config       # 桌面入口（图标跳哪个端口/路径）
│   └── cmd/                # 生命周期脚本（start/stop/status + 各阶段回调）
├── assets/                 # 图标源（make-icons.py 生成）
├── build.mjs               # 打包脚本（自写 tar，钉死权限位）
└── dist/                   # 产物 .fpk
```

生命周期脚本的约定（飞牛会定期轮询 `status` 判断应用状态）：

| 调用 | 退出码 |
|---|---|
| `main start` | 0 成功 / 1 失败 |
| `main stop` | 0 成功 / 1 失败 |
| `main status` | **0 = 运行中，3 = 未运行** |
